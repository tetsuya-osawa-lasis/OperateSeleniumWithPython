
!!!include(./ソースコード生成指示文面/#2ソースコード生成指示文面01.md)!!!

設計:
---

# Selenium管理クラス【Selenium操作を提供する管理クラス】

<br />

## プロパティ
なし

<br />

## メソッド

1. ### 【HTMLの「a」タグの中から、指定された文字列を含む最初のエレメントを取得する】

    #### 戻り値（型）
    エレメント（Seleniumのエレメント）

    #### 引数（型）
    - エレメント（Seleniumのエレメント）
    - 指定された文字列（文字列）

    #### 処理内容:
    1. エレメントに含まれる、「a」タグのリストを取得する。

    1. LOOP: 上記の「a」タグのリスト

        1. 「a」タグの配下に、引数で渡された「指定された文字列」が含まれる「a」タグを特定する。
    
        END
        
    ---

1. ### 【HTMLの「input」タグの中から、指定された文字列を含む最初のエレメントを取得する】

    #### 戻り値（型）
    エレメント（Seleniumのエレメント）

    #### 引数（型）
    - Webドライバ（SeleniumのWebドライバ）
    - 指定された文字列（文字列）

    #### 処理内容:
    - HTMLに含まれる、すべての「input」タグを取得する。（LIST型）
    - 上記の「input」タグのリストに対して、ループ処理を行う。
    - ループ処理の中で、「input」タグの配下に、引数で渡された「指定された文字列（nameの属性）」が含まれる「input」タグを特定する。

    ※工藤（１番上のメソッドに、記述を合わせる）

    ---

1. ### 【HTMLの「button」タグの中から、指定された文字列を含む最初のエレメントを取得する】

    ### 戻り値（型）
    エレメント（Seleniumのエレメント）

    ### 引数（型）
    - Webドライバ（SeleniumのWebドライバ）
    - 指定された文字列（文字列）

    ### 処理内容:
    - HTMLに含まれる、すべての「button」タグを取得する。（LIST型）
    - 上記の「button」タグのリストに対して、ループ処理を行う。
    - ループ処理の中で、「button」タグの配下に、引数で渡された「指定された文字列」が含まれる「button」タグを特定する。

    ※工藤（１番上のメソッドに、記述を合わせる）

    ---

1. ### 【HTMLの「dd」タグの中から、指定された文字列を含む最初のエレメントを取得する】

    ### 戻り値（型）
    エレメント（Seleniumのエレメント）

    ### 引数（型）
    - Webドライバ（SeleniumのWebドライバ）
    - 指定された文字列（文字列）

    ### 処理内容:
    - HTMLに含まれる、すべての「dd」タグを取得する。（LIST型）
    - 上記の「dd」タグのリストに対して、ループ処理を行う。
    - ループ処理の中で、「dd」タグの配下に、引数で渡された「指定された文字列」が含まれる「dd」タグを特定する。

    ※工藤（１番上のメソッドに、記述を合わせる）

    ---



